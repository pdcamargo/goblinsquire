import React from 'react';

const Editor: React.FC = () => {
  return (
    <div
      style={{
        width: '100%',
        marginBottom: '150px',
        padding: '15px',
        marginLeft: '15px',
      }}
    />
  );
};

export default Editor;
