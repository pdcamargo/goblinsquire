import { FontWeightType } from '../types';

const fontWeights: Record<FontWeightType, number> = {
  thin: 100,
  light: 300,
  normal: 400,
  medium: 500,
  bold: 700,
  extrabold: 800,
  black: 900,
};

export default fontWeights;
